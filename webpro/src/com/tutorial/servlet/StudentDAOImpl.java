package com.tutorial.servlet;

import java.sql.SQLException;

public class StudentDAOImpl {
	// All the db connection
	// getEmployeeInfo(String fn , String ln){
	
 //}
	
	/*public List selectQuery(int salary) {
		Employee emp = null;
		List list = new ArrayList();
		try{
			stmt = con.createStatement(); // Step 3
			rs = stmt.executeQuery("select * from employees where salary > "+empid); // Step 4
			while(rs.next()){
				emp = new Employee();
				emp.setName(rs.getString("FirstName"));
				emp.setDesignation(rs.getString("Designation"));
				list.add(emp);
			}
		}catch(SQLException exp){
			exp.printStackTrace();
		}finally{
			closeConnections(rs,stmt,con);
		}
		return list;
	}*/
}
